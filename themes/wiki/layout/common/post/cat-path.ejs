<%- list_categories(post.categories, {
            show_count: false,
            style: false,
            separator: '/',
            transform(str) {
            return strip_html(str);
            }
}) %>
